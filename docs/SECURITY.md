# Security

- PII handling strategy: `PII_REDACTION_MODE` is planned for M5.
- No PII should be written to logs.
- Secrets are provided through environment variables.
- CORS is restricted to known origins.
- Authentication is not implemented in V1 (future work).
